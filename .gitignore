# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# Docker and Secrets
.env
volumes/
letsencrypt/ # Keeping this just in case, even if Traefik is external

# Frontend specific ignores
frontend/node_modules/
frontend/build/
frontend/.env          # Ignore any .env file directly in frontend/ (should be empty for production builds anyway)
frontend/.env.local    # Ignore local development env files

# WordPress specific ignores (if not managed by Docker volumes)
# wp-content/uploads/ # Uncomment if your uploads are not in a Docker volume
